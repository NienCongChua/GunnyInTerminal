# GunnyInTerminal

A tiny Gunny-style game that runs directly in your terminal.

## Run

```bash
python3 gunny_game.py
```

Enter shots as:

```text
<angle> <power>
```

Example:

```text
45 25
```

Type `q` to quit.

## Test

```bash
python3 -m unittest discover -s tests
```
